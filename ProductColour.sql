CREATE TABLE [dbo].[ProductColour] (
    [pcProductColourID] INT IDENTITY (1, 1) NOT NULL,
    [pcProductID]    VARCHAR (100),
    [pcColourID]  VARCHAR (100),
    [pcImage] VARCHAR (100),
    [pcSortOrder] VARCHAR (100),
);

